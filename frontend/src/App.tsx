import { BrowserRouter } from 'react-router';
import Router from '@/components/routes/Router';

function App() {
  return (
    <BrowserRouter>
      <Router />
    </BrowserRouter>
  );
}
export default App;
